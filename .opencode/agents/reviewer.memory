# Reviewer Memory

## Log

<!-- One line per durable review event. Most recent first. -->
- 2026-05-18: Final SVAMP review passed. Verified 9 tests, config-only CLI help, `outputs/datasets/svamp.jsonl` has 4 accepted records with `Answer` metadata and 0 secret-pattern hits; rejected audit has 1 sanitized `no_next_kept_unit` record.
- 2026-05-18: Reviewed SVAMP final-output cleanup. Verified only `outputs/poc/svamp_gemma4_gemini_flash_lite.jsonl` remains under outputs, with 18 structurally valid accepted HF records and no obvious secret patterns.
- 2026-05-17: Reviewed Data repo POC implementation. Offline pytest and seed/mock CLI smoke paths passed; optional LiteLLM/Hugging Face paths fail closed with install hints when extras are absent.

## Structured Knowledge

<!-- Stable review rules, recurring risk patterns, and verification expectations. -->
- POC review baseline: `uv run --extra dev python -m pytest`, module CLI seed/mock run, console-script seed/mock run, and structural JSONL parse all pass without API keys/network.
